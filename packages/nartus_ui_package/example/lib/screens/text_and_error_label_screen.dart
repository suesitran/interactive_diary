import 'package:flutter/material.dart';

class TextAndErrorLabelScreen extends StatelessWidget {
  const TextAndErrorLabelScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Text and Error Label demo'),
      ),
      body: Container(
        alignment: Alignment.center,
        child: Text('T.B.D'),
      ),
    );
  }
}
