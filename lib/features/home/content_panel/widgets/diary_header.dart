part of 'content_card_view.dart';

class _DiaryHeader extends StatelessWidget {
  const _DiaryHeader({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder(

    );
  }
}
