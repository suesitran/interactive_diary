part of 'content_card_view.dart';

class _DiaryThumbnailContent extends StatelessWidget {
  const _DiaryThumbnailContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
